name: Test self-hosted runner

on:
  push:

jobs:
  test:
    runs-on: [self-hosted, linux, x64]
    steps:
      - name: Check runner
        run: |
          echo "Runner works!"
          echo "Host: $(hostname)"
          echo "Date: $(date)"
      - name: Check docker
        run: docker --version
